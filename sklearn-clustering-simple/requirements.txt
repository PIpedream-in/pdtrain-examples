# Dependencies are pre-installed in SageMaker scikit-learn container
# Only add packages not included in the base image
# scikit-learn==1.2.1 (pre-installed)
# pandas==1.1.3 (pre-installed)
# numpy==1.24.1 (pre-installed)
# joblib==1.5.1 (pre-installed)
